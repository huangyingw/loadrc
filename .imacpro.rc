#alias restartWifi='$HOME/loadrc/osxrc/restart_wifi.sh'
#export PATH="$HOME/anaconda3/bin:$PATH"

if [ $(uname) = "Darwin" ]
then
    if ! (sudo systemsetup -getcomputersleep | grep -q "after 1 minutes") ; \
        then \
        sudo systemsetup -setcomputersleep 1; \
        fi
fi
