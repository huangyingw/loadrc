#!/bin/bash
OS=`uname`
if [ $OS != "Darwin" ];
then
    return
fi
sudo pmset -a sleep 50
sudo pmset -a displaysleep 50
chmod 400 ~/.ssh/id_rsa
chmod 400 ~/loadrc/keys/*.pem
alias restartBluetooth='$HOME/loadrc/osxrc/restart_bluetooth.sh '
