#!/bin/bash
. $HOME/loadrc/.androidrc
. $HOME/loadrc/.dockerrc
. $HOME/loadrc/.envrc
. $HOME/loadrc/.gitrc
. $HOME/loadrc/.hgrc
. $HOME/loadrc/.kvmrc
. $HOME/loadrc/.p4rc
. $HOME/loadrc/.osxrc
. $HOME/loadrc/.shellrc
. $HOME/loadrc/.sqlrc
. $HOME/loadrc/.svnrc
. $HOME/loadrc/openrc.sh

HOSTRC=$HOME/loadrc/."`hostname`"rc
if [ -f "$HOSTRC" ]
then
  . "$HOSTRC"
fi
