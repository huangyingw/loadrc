#!/bin/bash
. $HOME/loadrc/.androidrc
. $HOME/loadrc/.dockerrc
. $HOME/loadrc/.envrc
. $HOME/loadrc/.gitrc
. $HOME/loadrc/.hgrc
. $HOME/loadrc/.kvmrc
. $HOME/loadrc/."`hostname`".osxrc
. $HOME/loadrc/.p4rc
. $HOME/loadrc/.pythonrc
. $HOME/loadrc/.shellrc
. $HOME/loadrc/.sqlrc
. $HOME/loadrc/.svnrc
. $HOME/loadrc/.zerotierrc
. $HOME/loadrc/openrc.sh

HOSTRC=$HOME/loadrc/."`hostname`".rc
if [ -f "$HOSTRC" ]
then
    . "$HOSTRC"
fi
